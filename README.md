# Gemini API Python Project

This is a simple Python project that uses **Google Gemini API** securely using environment variables.

🚫 API keys are **NOT hardcoded**  
✅ Secrets are managed using a `.env` file  
✅ Safe to push on GitHub

---

## 📁 Project Structure

# Gemini API Python Project

This is a simple Python project that uses **Google Gemini API** securely using environment variables.

🚫 API keys are **NOT hardcoded**  
✅ Secrets are managed using a `.env` file  


---

## 🔐 Environment Variables

This project uses a `.env` file to store sensitive data.

### Create `.env`
Create a file named `.env` in the root folder:

```env
GEMINI_API_KEY=your_api_key_here
🧪 Setup Instructions
1️⃣ Clone the repository
 git clone https://github.com/appump/Chatbot.git
cd your-repo-name

2️⃣ Create virtual environment (optional but recommended)
python -m venv venv

Activate:
Windows
venv\Scripts\activate

Linux / macOS
source venv/bin/activate

3️⃣ Install dependencies
pip install python-dotenv
pip install google-genai

4️⃣ Run the application
python app.py

